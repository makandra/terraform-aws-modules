name: Checkov
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: makandra/github-actions/checkov-terraform@main
        with:
          path: modules/
